// Fixture file so VS Code opens a real folder (not an empty window) during e2e.
// Gives the extension an active text editor and a selection target.
export function add(a: number, b: number): number {
  return a + b;
}
